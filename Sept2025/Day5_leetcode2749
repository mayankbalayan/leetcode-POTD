2749. Minimum Operations to Make the Integer Zero


https://leetcode.com/problems/minimum-operations-to-make-the-integer-zero/description/?envType=daily-question&envId=2025-09-05


class Solution {
public:
    int makeTheIntegerZero(int num1, int num2) {
     //0=num1-k(2^i+num2)
     //k*2^i=num1-k*num2//x=sum of k times 2^i for different i
     //x=num1-k*nums2;
     //i can be different in each operation 
     //k can be anything from 1 to 60 so we check for every k if x=of the form 2^i
     for(int k=1;k<60;k++){
        long long x=1LL*num1-1LL*k*num2;
        if(x<0)continue;//x cannot be negative
        //we will use popcount function to determine minimum value of k
        //as x=k*2^i so popcount calculates min no. of 1 in x that shows different power of 2 used and value of k
        if(__builtin_popcountll(x)<=k&&k<=x)return k;
     }
     return -1;
    }
};
